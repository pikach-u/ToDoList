spring:
  application:
    name: toDoList

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: toDoList App
    version: 1.0.0
